# Infrastructure

This directory is reserved for versioned Supabase and AWS infrastructure definitions, policy tests,
and deployment configuration.

No infrastructure is provisioned by this directory migration. The active OpenSpec changes define
the required Mumbai Postgres, private storage, KMS, SQS, SNS, Textract, Bedrock, retention, and
monitoring boundaries.
