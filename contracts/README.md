# API Contracts

This directory is the shared boundary between the backend and web, iOS, and Android clients.

The backend's OpenAPI document will remain the source for generated client types. No checked-in
contract or generated client exists yet; those belong to a later API-contract task once the V1
endpoints are defined.
