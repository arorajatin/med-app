# Web Client

This directory is the home of the V1 web client described by the repository's user journeys.

No framework or application scaffold has been selected yet. That choice should be made separately
from this directory migration and should consume a versioned API contract from `contracts/`.
